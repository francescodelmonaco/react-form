import ArticlesList from "./ArticlesList";
import Form from "./Form";

const Main = () => {
    return (
        <main>
            <ArticlesList />
            <Form />
        </main>
    )
};

export default Main;